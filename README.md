# Project NOVA — A Helping Hand
Run: `npm install` then `npm run dev`. Build: `npm run build`.
Pages: / (home), /register, /login, /dashboard, /admin. All copy lives in src/data.js.
Data is stored in localStorage (demo only) — connect Supabase/Firebase for real auth, storage and admin roles.
